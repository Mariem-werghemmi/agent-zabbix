# ---------------------------------------------------------------------------
# Agent IA - Supervision Zabbix N1
# Dependances Python. Installation : pip install -r requirements.txt
# ---------------------------------------------------------------------------
# Client officiel Zabbix (gere l'authentification 6.x ET 7.x automatiquement)
zabbix-utils==2.0.2
# SDK compatible OpenAI : fonctionne avec Groq, Gemini, Mistral ET Ollama.
# C'est cette compatibilite qui permet de basculer cloud <-> local
# en changeant uniquement le fichier .env (aucune modification de code).
openai==1.68.2
# Validation des donnees + lecture de la configuration depuis .env
pydantic==2.11.3
pydantic-settings==2.8.1
# Console web
fastapi==0.115.12
uvicorn[standard]==0.34.0
jinja2==3.1.6
# Utilitaires
python-dotenv==1.1.0
httpx==0.28.1
